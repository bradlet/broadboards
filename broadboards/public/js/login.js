// This file contains the function controlling
// login.html's submit button.

// API routes
const checkUsernameAPI = "/checkUsernameExists/";
const checkPWAPI = "/checkPassword";

const attemptLogin = function() {
  /*
  const response = await fetch(checkUsernameAPI+
    '/'+startingNumOfThreads);
  // console.log(response)
  const body = await response.json();
  // console.log(body)

  if (response.status !== 200) {
    throw Error(body.message)
  }
  return body;
  */
};
